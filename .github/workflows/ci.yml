name: CI

on:
  push:
<<<<<<< HEAD
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: cardapio_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DB_HOST: localhost
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: cardapio_db
      JWT_SECRET: test-secret
      NEXT_PUBLIC_API_URL: http://localhost:3001

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Instalar PNPM
        uses: pnpm/action-setup@v4
=======
<<<<<<< HEAD
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  DB_HOST: localhost
  DB_PORT: 5432
  DB_USER: postgres
  DB_PASSWORD: postgres
  DB_NAME: cardapio_test
  JWT_SECRET: test-secret-key
  NODE_ENV: test

jobs:
  backend:
    name: Backend - TypeScript & TypeORM
=======
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy para Produção
>>>>>>> f917d5406f412d6fc87a7f46ac6cf93f79d4b534
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
<<<<<<< HEAD
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: cardapio_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Build TypeScript
        working-directory: backend
        run: npm run build

      - name: Lint backend
        working-directory: backend
        run: npm run lint || true

      - name: Build Docker image
        run: docker build -t cardapio-api:latest -f backend/Dockerfile backend/

  frontend:
    name: Frontend - Next.js
    runs-on: ubuntu-latest
=======
    environment:
      name: production
>>>>>>> f917d5406f412d6fc87a7f46ac6cf93f79d4b534

    steps:
      - uses: actions/checkout@v4

<<<<<<< HEAD
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: package.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Next.js
        run: npm run build

      - name: Lint frontend
        run: npm run lint || true

      - name: Build Docker image
        run: docker build -t cardapio-frontend:latest -f Dockerfile.frontend .

  docker-compose:
    name: Docker Compose Build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Test Docker Compose build
        run: docker-compose -f docker-compose.dev.yml build

      - name: Verify services start
        run: |
          docker-compose -f docker-compose.dev.yml up -d
          sleep 5
          docker-compose -f docker-compose.dev.yml ps
          docker-compose -f docker-compose.dev.yml down
=======
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Backend Image
        uses: docker/build-push-action@v5
>>>>>>> b3a51faee96eb04a835fb3582515aa626c95d6eb
        with:
          version: 9

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Instalar dependências do backend
        working-directory: ./backend
        run: pnpm install

      - name: Lint backend
        working-directory: ./backend
        run: pnpm run lint || echo "Lint não configurado"

<<<<<<< HEAD
      - name: Instalar dependências do frontend
        working-directory: ./frontend
        run: pnpm install

      - name: Build frontend
        working-directory: ./frontend
        run: pnpm run build

      - name: Lint frontend
        working-directory: ./frontend
        run: pnpm run lint || echo "Lint não configurado"

      - name: Build das imagens Docker (modo dev)
        run: docker-compose -f docker-compose.dev.yml build
=======
      - name: Notificar deploy
        run: |
          echo "✅ Deploy realizado com sucesso!"
          echo "📦 Backend: cardapio-backend:latest"
          echo "📦 Frontend: cardapio-frontend:latest"
>>>>>>> f917d5406f412d6fc87a7f46ac6cf93f79d4b534
>>>>>>> b3a51faee96eb04a835fb3582515aa626c95d6eb
