name: ci
run-name: Pipeline de integração contínua e análise de código

on: 
  push: 
    branches:           
      - main

jobs:
  ci: 
    name: Build and Analyze
    runs-on: ubuntu-latest

    steps:          
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci
        working-directory: integration-test

      - name: Install Cypress binary (headless CI-safe)
        run: npx cypress install
        working-directory: integration-test

      - name: Run Cypress tests (Electron, headless, CI)
        run: npx cypress run --browser electron --headless
        working-directory: integration-test

      - name: SonarQube Analysis
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          args: >
            -Dsonar.projectKey=cardapio-digital-tentativa15
            -Dsonar.projectName=cardapio-digital
            -Dsonar.projectVersion=1.0
            -Dsonar.sources=.